@model IEnumerable<Domain.EntregaDocuentos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DocumentosId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AlumnoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Entregado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DocumentosId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AlumnoId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Entregado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EntregaDocuentosId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EntregaDocuentosId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EntregaDocuentosId })
        </td>
    </tr>
}

</table>
